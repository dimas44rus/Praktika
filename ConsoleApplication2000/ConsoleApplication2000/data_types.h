#pragma once

// Структура, объединяющая два целых числа в один объект.
// Именно этот тип данных будет храниться внутри каждого элемента нашего стека.
struct IntPair {
    int first;   // Первое число пары
    int second;  // Второе число пары
};
