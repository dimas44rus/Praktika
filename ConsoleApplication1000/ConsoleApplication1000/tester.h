#pragma once
#include "stack.h"
#include "data_types.h"

// Функции для быстрого заполнения тестовыми данными
void fillDemoIntStack(elem<int>*& stack);
void fillDemoStudentStack(elem<Student>*& stack);
