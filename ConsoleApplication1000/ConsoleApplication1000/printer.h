#pragma once
#include "stack.h"
#include "data_types.h"

// Прототипы перегруженных функций для печати содержимого стека
void printStack(const elem<int>* stack);
void printStack(const elem<Student>* stack);
