#ifndef STRING_MODIFIER_H
#define STRING_MODIFIER_H

#include <string>

class StringModifier {
public:
    void capitalizeWords();    // Метод для Задачи 4 (цикл по ссылке)
    void insertAndReplace();   // Метод для Задачи 5 (.insert, .replace)
    void extractAndErase();    // Метод для Задачи 6 (.substr, .erase)
};

#endif
