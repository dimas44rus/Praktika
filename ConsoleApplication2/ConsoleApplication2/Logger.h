#ifndef LOGGER_H // Защита от повторного включения заголовочного файла логирования действий системы.
#define LOGGER_H // Определение макроса для защиты от циклической зависимости при компиляции.

#include <string> // Подключение стандартных строк для передачи текстовых сообщений в методы логера.

class Logger { // Объявление вспомогательного сервисного класса Logger для записи действий пользователей в файл истории.
public: // Публичный доступ к методам логера для вызова из любой точки программы.
    static void logAction(const std::string& userRole, const std::string& action); // Статический метод для фиксации роли пользователя и выполненного им действия с временной меткой.
}; // Конец описания структуры класса логера.

#endif // Завершение блока директивы #ifndef.
