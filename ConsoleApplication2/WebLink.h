#pragma once

// Класс "WebLink" (Веб-ссылка) - демонстрация РАБОТЫ С ДИНАМИЧЕСКОЙ ПАМЯТЬЮ
// Строки хранятся не в статических массивах, а в динамически выделенной памяти
// Это позволяет экономить память и хранить строки любой длины
class WebLink
{
private:
    char* url;          // Указатель на динамический массив для URL-адреса
    char* description;  // Указатель на динамический массив для описания

public:
    // КОНСТРУКТОР - специальный метод, который вызывается автоматически
    // при создании объекта. Имя конструктора совпадает с именем класса.
    // Задача конструктора - инициализировать объект (выделить память, задать начальные значения)
    WebLink(const char* linkUrl, const char* linkDesc);

    // ДЕСТРУКТОР - специальный метод, который вызывается автоматически
    // при уничтожении объекта. Имя деструктора: ~ИмяКласса
    // Задача деструктора - освободить ресурсы (память, файлы, соединения)
    // Деструктор не имеет параметров и не возвращает значение
    ~WebLink();

    // Метод для вывода информации о ссылке
    void print();
};